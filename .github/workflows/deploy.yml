name: 🚀 Deploy Webeez Legacy (Theme & Plugin Only)

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy to Live Server
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      # ----------------------------------------------------------------
      # ΒΗΜΑ 1: Ανέβασμα του THEME (Webeez)
      # ----------------------------------------------------------------
      - name: 🎨 Deploy Webeez Theme
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp

          # Ποιον φάκελο παίρνουμε από το GitHub;
          local-dir: ./wp-content/themes/webeez/

          # Πού το βάζουμε στον Server; (Τσέκαρε το path!)
          server-dir: ./webeez.gr/public_html/wp-content/themes/webeez/

          # Τι ΝΑ ΜΗΝ ανεβάσει (Εξαιρέσεις)
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .DS_Store
            assets/src/** # <--- Η ΕΞΑΙΡΕΣΗ ΠΟΥ ΖΗΤΗΣΕΣ (To src δεν ανεβαίνει)

      # ----------------------------------------------------------------
      # ΒΗΜΑ 2: Ανέβασμα του PLUGIN (Webeez Addons)
      # ----------------------------------------------------------------
      - name: 🧩 Deploy Webeez Addons Plugin
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp

          # Ποιον φάκελο παίρνουμε;
          local-dir: ./wp-content/plugins/webeez-addons/

          # Πού το βάζουμε;
          server-dir: ./webeez.gr/public_html/wp-content/plugins/webeez-addons/

          exclude: |
            **/.git*
            **/.git*/**
            .DS_Store
